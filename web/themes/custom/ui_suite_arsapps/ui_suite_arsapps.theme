<?php

/**
 * @file
 * ARS Apps Theme theme file.
 */

/**
 * Implements hook_theme_suggestions_views_view_fields_alter().
 */
function ui_suite_arsapps_theme_suggestions_views_view_fields_alter(array &$suggestions, array $variables) {
  $view = $variables['view'] ?? NULL;
  if ($view) {
    $suggestions[] = 'views_view_fields__' . $view->id() . '__' . $view->current_display;
  }
}
